package org.omnetpp.samples.animation.widgets;

import org.eclipse.swt.widgets.Composite;
import org.omnetpp.animation.widgets.AnimationCanvas;

public class CustomAnimationCanvas extends AnimationCanvas
{
    public CustomAnimationCanvas(Composite parent, int style) {
        super(parent, style);
    }
}
