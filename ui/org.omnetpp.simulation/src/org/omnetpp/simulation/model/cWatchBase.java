package org.omnetpp.simulation.model;

import org.omnetpp.simulation.controller.Simulation;

/**
 * Represents objects derived from the cWatchBase C++ class in the simulation.
 *
 * @author Andras
 */
//TODO finish
public class cWatchBase extends cObject {
    public cWatchBase(Simulation simulation, long id) {
        super(simulation, id);
    }
}
