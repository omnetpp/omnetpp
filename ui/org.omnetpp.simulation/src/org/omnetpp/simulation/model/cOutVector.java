package org.omnetpp.simulation.model;

import org.omnetpp.simulation.controller.SimulationController;

public class cOutVector extends cObject {
    public cOutVector(SimulationController controller, long id) {
        super(controller, id);
    }
}
