package org.omnetpp.scave2.actions;

import org.eclipse.jface.viewers.IStructuredSelection;
import org.omnetpp.scave2.editors.ScaveEditor;

/**
 * Creates a dataset from the filter setting on the BrowseDataPage and 
 * creates a chart displaying the dataset. 
 */
public class CreateChartActionDelegate extends AbstractScaveActionDelegate {

	@Override
	protected void doRun(ScaveEditor editor,
			IStructuredSelection selection) {
		// TODO
	}

	@Override
	protected boolean isEnabled(ScaveEditor editor,
			IStructuredSelection selection) {
		// TODO
		return false;
	}
}
