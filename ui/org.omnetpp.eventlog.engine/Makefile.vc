#
# Global definitions
#
OMNETPP_ROOT=..\..

!include $(OMNETPP_ROOT)\configuser.vc

JDK="$(TOOLS_DIR)\jdk1.5.0"
SWIG="$(TOOLS_DIR)\SWIG-1.3.25\swig.exe"

COMMON=$(OMNETPP_ROOT)\src\common
EVENTLOG_ENGINE=$(OMNETPP_ROOT)\src\eventlog


#
# Local definitions
#
COPTS=$(CFLAGS) -I$(JDK)\include -I$(JDK)\include\win32 -I$(EVENTLOG_ENGINE) -I$(COMMON)

OBJS = engine_wrap.obj
HEADERS = $(EVENTLOG_ENGINE)\*.h $(COMMON)\*.h
JAVAPKG = org.omnetpp.eventlog.engine
JAVADIR = src\$(JAVAPKG:.=\)

#
# Automatic rules
#
.SUFFIXES : .cc

.cc.obj:
	$(CXX) /Fo"$@" $(COPTS) /c /Tp $<

.cxx.obj:
	$(CXX) /Fo"$@" $(COPTS) /c /Tp $<


#
# Targets
#
default: eventlog_engine.dll

engine_wrap.cxx: engine.i $(HEADERS)
	-del $(JAVADIR)\*.java >nul
	$(SWIG) -c++ -java -package $(JAVAPKG) -I$(EVENTLOG_ENGINE) -I$(COMMON) -outdir $(JAVADIR) engine.i

eventlog_engine.dll : $(OBJS)
	$(LINK) /dll /out:"eventlog_engine.dll" $(OBJS) $(EVENTLOG_ENGINE)/eventlog.lib $(COMMON)/common.lib $(LDFLAGS)

depend:
	$(MAKEDEPEND) -I../../include -fMakefile.vc -- *.cc

clean:
	-del engine_wrap.cxx *.obj *.exp *.lib test*.exe eventlog_engine.dll 2> nul
	-del *.ilk *.idb *.pdb *.pch 2> nul

# DO NOT DELETE THIS LINE -- make depend depends on it.

