#
# rpm-install-cfg:
#
# This file is used as configure.user during building RPM releases.
#

#
# Switches:
#
CFLAGS='-g -Wall -Wno-unused'
#CFLAGS='-O3 -DNDEBUG=1 -Wno-unused'

build_shared_libs=yes

TKENV_EMBED_TCL=no
GNED_EMBED_TCL=no

PVM_CFLAGS=
PVM_LIBS=

TK_CFLAGS=
TK_LIBS=
#TK_LIBS='-ltk80 -ltcl80 -lX11'


#
# Directories: use installation directories
#
OMNETPP_ROOT="$OMNETPP_INSTALL_ROOT"

OMNETPP_SRC_DIR="$OMNETPP_ROOT/src"
OMNETPP_SAMPLES_DIR="$OMNETPP_ROOT/samples"
OMNETPP_BITMAP_PATH=".;./bitmaps;$OMNETPP_ROOT/bitmaps"

OMNETPP_BIN_DIR="$OMNETPP_ROOT/bin"
OMNETPP_INCL_DIR="$OMNETPP_SRC_DIR/sim"
OMNETPP_LIB_DIR="$OMNETPP_ROOT/lib"

