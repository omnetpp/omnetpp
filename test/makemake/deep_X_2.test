%description:
Test the --deep option with -X. (don't build anything)

%file: top.msg
some syntax error

%file: a/a.msg
another syntax error


%testprog: work -h classes
%ignore-exitcode: 1
%file: buildspec
all:
	$(OPP_MAKEMAKE) --deep -X.

%contains: stdout
class cMessage

