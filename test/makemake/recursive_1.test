%description:
Test the -r (recursive) option. It should visit every subdirectory.

%file: top.msg
message Top {}

%file: a/a.msg
message A {}

%file: b/b.msg
message B {}

%file: build
all:
	$(OPP_MAKEMAKE) -r -La -Lb -laaa -lbbb
	cd a && $(OPP_MAKEMAKE) -a -o aaa
	cd b && $(OPP_MAKEMAKE) -a -o bbb

%contains: classlist.out
class A
%contains: classlist.out
class B
%contains: classlist.out
class Top

