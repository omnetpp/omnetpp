%description:
Tests scenario generation: parallel iterations: not enough values

%inifile: omnetpp.ini
[Config Test]
**.a = ${i=1..4}
**.b = ${100,200 ! i}

%extraargs: -G -x Test

%contains: stdout
XXX (currently asserts...)
