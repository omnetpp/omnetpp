%description:
Tests scenario generation: parallel iterations: alpha values

%inifile: omnetpp.ini
[Config Test]
**.a = ${i=1..4}
**.b = ${alpha,beta,gamma,delta ! i}

%extraargs: -G -x Test

%contains: stdout
XXX
