#
# When loaded in addition to omnetpp.ini, this ini file ensures that networks
# will be layouted with a fixed background size, by setting parameters
# of the "Base" module that every network in this folder extends.
#

[General]
*.bgWidth = "800"
*.bgHeight = "600"