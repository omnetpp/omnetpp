#
# When loaded in addition to omnetpp.ini, this ini file adds some fixed nodes 
# to the network. It works by setting parameters of the "Base" module that 
# every network in this folder extends.
#

[General]
*.numFixed = 4
