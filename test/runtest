#! /bin/sh
[ ! -d work ] && mkdir work
perl opp_test.pl -g -v *.test
echo
(cd work; opp_makemake -f -u cmdenv; make) || exit 1
echo
perl opp_test.pl -r -v *.test

echo
echo Results can be found in ./work
echo