%description:
Test "like" connections: "no such channel type"

%file: test.ned

channelinterface IFooChannel
{
}

module Node
{
    gates:
        inout p;
    connections allowunconnected:
}

network Test
{
    parameters:
        string channelType = "foo";
    submodules:
        a: Node;
        b: Node;
    connections:
        a.p <--> <channelType> like IFooChannel <--> b.p;
}

%exitcode: 1

%contains-regex: stdout
No channel type named `foo'

