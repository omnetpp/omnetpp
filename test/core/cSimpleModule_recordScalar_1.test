%description:
check that recordScalar() methods work OK.

%activity:

recordScalar("one", 24.2);
recordScalar("two", -1.3);

%contains: {}.sca
run 1 "{}_network"
scalar "{}_network" 	"one" 	24.2
scalar "{}_network" 	"two" 	-1.3

