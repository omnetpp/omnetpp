%description:
check that recordScalar() methods work OK.

%activity:

recordScalar("one", 24.2);
recordScalar("two", "some text");

%contains: {}.sca
run 1 "{}_network"
scalar "{}_network" 	"one" 	24.2
scalar "{}_network" 	"two" 	"some text"

