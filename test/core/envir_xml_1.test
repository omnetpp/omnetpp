%description:
Check ev.getXMLDocument() basic functionality
Note: path expr testing is in cXMLElement_1.test.

%file: test.xml

<foo id="1">
    <foo id="2" color="red">one</foo>
    <bar id="3" color="red">
        <foo id="4">two</foo>
        <bar id="5">three</bar>
    </bar>
</foo>

%activity:

cXMLElement *node = getEnvir()->getXMLDocument("test.xml");
if (!node)
    EV << "nullptr\n";
else
    EV << node->getTagName() << " from " << node->getSourceLocation() << "\n";

%contains: stdout
foo from test.xml:2
