@echo off
rem
rem Renaming all .cc files to .cpp for building the samples from the MSVC IDE.
rem

if exist .cpps exit
echo dummy file generated by cc2cpp.bat > .cpps

for %%i in (aloha cqn dyna fddi fifo hcube hist neddemo p-to-p queueing routing routingdb tictoc tokenring) do ren %%i\*.cc *.cpp
