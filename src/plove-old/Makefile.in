#
# variables set by `configure':
#
OMNETPP_BIN_DIR=@OMNETPP_BIN_DIR@

depend:

clean:

install-local: plove
	(dir=`pwd`; cd $(OMNETPP_BIN_DIR); ln -s $$dir/plove .)

# DO NOT DELETE THIS LINE -- make depend depends on it.
