#! /bin/sh
NEDDOC_XSL=`dirname $0`/neddoc.xsl
TMPFILE=/tmp/opp_neddoc.tmp.$$

cleanup()
{
    rm $TMPFILE
}

trap cleanup HUP INT QUIT TERM

nedtool -x -e -y -m -o $TMPFILE $*
xsltproc $NEDDOC_XSL $TMPFILE
cleanup

